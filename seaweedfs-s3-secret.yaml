# SeaweedFS S3 Secret
# Apply this before deploying SeaweedFS
apiVersion: v1
kind: Secret
metadata:
  name: seaweedfs-s3-secret
  namespace: seaweedfs
type: Opaque
stringData:
  # S3 credentials configuration
  # IMPORTANT: Change these values for production!
  config.json: |
    {
      "identities": [
        {
          "name": "mimir",
          "credentials": [
            {
              "accessKey": "mimir-access-key",
              "secretKey": "mimir-secret-key-change-me"
            }
          ],
          "actions": [
            "Admin",
            "Read",
            "List",
            "Tagging",
            "Write"
          ]
        }
      ]
    }
